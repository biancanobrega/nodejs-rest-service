/**
 * This enum contains the name for the endpoints defined in the json file per-environment.
 * The endpoints files are inside the config folder.
 */
export enum Endpoint {
  ExampleMongo = 'example_mongo',
}
